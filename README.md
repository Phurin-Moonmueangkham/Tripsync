1. ติดตั้ง dependencies ทั้งหมด
```
npm install
```

2. ติดตั้ง packages เพิ่มเติมที่จำเป็น

```
npx expo install react-native-maps@1.20.1 react-native-screens@4.16.0
npm install zustand
npx expo install react-dom react-native-web @expo/metro-runtime
```

3. รันแอป

```
npx expo start
```
